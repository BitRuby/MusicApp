import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  title: {
    textAlign: 'center',
    paddingVertical: 10,
    color: '#fff',
    fontSize: 16,
  },
});

export default styles;
